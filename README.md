# Link-Extractor
 Multithreaded Producer/Consumer Web Scraper  This is a Python-based, multithreaded **web link extractor** built using a **Producer–Consumer architecture**. It fetches HTML from a list of websites, extracts all the hyperlinks, and saves them to a file — all while avoiding IP bans through responsible scraping practices.
